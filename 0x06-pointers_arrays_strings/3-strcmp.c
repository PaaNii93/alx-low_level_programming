#include "main.h"
#include <string.h>

/**
 * _strcmp - function compares two strings.
 * @s1: string 1
 * @s2: string 2
 * Return: 0.
 */

int _strcmp(char *s1, char *s2)
int _putchar(int *a)

{	
	a = strcmp(s1, s2);

	if (a < 0)
	{
		putchar("s1 is less than s2");
	}
	else if (a > 0)
	{
		putchar("s1 is greater than s2");
	}
	else 
	{
		putchar("s1 iss equal to s2");
	}

	return (0);
}

