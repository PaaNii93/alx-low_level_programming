#include "main.h"
#include <ctype.h>

/**
 * string_toupper - function changes lowercase of a string to uppercase.
 * @a: pointer of string.
 * Return: 0
 */

char *string_toupper(char *)
}
	string_touper;
	return (0);
}
