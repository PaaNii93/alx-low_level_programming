#include <stdio>
#include "main.h"

/**
 * *_strcat - concantenate strings.
 */

void char *_strcat(char *dest, char *src)
{
	s1[] = char *dest
	s2[] = char *src
	int i;
	int j;

	for (i = 0, s1[i] != '\0', i++);
	for (j = 0, s2[j] != '\0', j++);
	{
		s1[i] = s2[j];
	}
	s1[i] = '\0';
