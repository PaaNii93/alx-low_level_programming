#include <stdio.h>
#include "main.h"

/**
 * _strlen - functions returns length of a string
 * @s: pointer for counting length of string
 * Return: int i
 */

int _strlen(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		i++
	}

	return (i);
}
