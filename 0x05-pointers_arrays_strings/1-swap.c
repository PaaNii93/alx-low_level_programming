#include "main.h"
#include <stdio.h>

/**
 * swap_int - swap values of two integers.
 * @a: a pointer deference for a
 * @b: a pointer deference for b
 * int c: assigning the address of variable a to c
 * Return: 0 (success)
 */

void swap_int(int *a, int *b)
{
	int c = *b
	*a = *b
	*a = c
}
